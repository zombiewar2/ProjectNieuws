import { CapacitorConfig } from '@capacitor/cli';
import { SplashScreen } from '@capacitor/splash-screen';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'projectnieuws',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};


export default config;
